# Non-sensitive agent config — copy to functions/.env and fill in values
AGENT_ENV_ID=env_...
AGENT_ID=agent_...
AGENT_VERSION=1
VAULT_IDS=vlt_...
SLACK_BOT_ID=B...

# Sensitive — set via Secret Manager instead of this file:
#   firebase functions:secrets:set SLACK_BOT_TOKEN
#   firebase functions:secrets:set SLACK_SIGNING_SECRET
#   firebase functions:secrets:set ANTHROPIC_KEY
